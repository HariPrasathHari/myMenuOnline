# Nginx configuration for removing .html extension
# Add this to your server block

location / {
    try_files $uri $uri.html $uri/ =404;
}

# Redirect .html URLs to non-.html
if ($request_uri ~ ^/(.*)\.html$) {
    return 301 /$1;
}
